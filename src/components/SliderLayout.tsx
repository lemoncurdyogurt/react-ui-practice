import Slider from "./Slider";

const SliderLayout = () => {
  return (
    <div className="flex bg-mint w-full h-full items-center justify-center">
      <Slider />
    </div>
  );
};
export default SliderLayout;
